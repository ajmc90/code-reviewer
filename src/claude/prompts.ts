/**
 * Compatibility re-export. Source moved to ./prompts/ (shared.ts, system.ts,
 * specialists/*.ts, summary.ts, index.ts).
 * Safe to delete once every importer points at './prompts' directly.
 */
export * from './prompts/index';
