/**
 * Compatibility re-export. Source moved to ./controllers/reviewController.
 * Safe to delete once every importer points at './controllers/reviewController' directly.
 */
export * from './controllers/reviewController';
