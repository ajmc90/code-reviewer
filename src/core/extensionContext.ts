/**
 * Compatibility re-export. Source moved to ./events/extensionContext.
 * Safe to delete once every importer points at './events/extensionContext' directly.
 */
export * from './events/extensionContext';
