/**
 * Compatibility re-export. Source moved to ./messages/ (en.ts, es.ts, index.ts).
 * Safe to delete once every importer points at './messages' directly.
 */
export { messages, type Lang, type MsgKey } from './messages/index';
